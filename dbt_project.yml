name: 'my_new_project'
version: '1.0.0'
config-version: 2

profile: 'my_new_project'

model-paths: ["models"]

models:
  my_new_project:

    Staging:
      +database: SAMPLE_PROJECT

    Int:
      +database: SAMPLE_PROJECT

    Mart:
      +database: SAMPLE_PROJECT